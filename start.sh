#!/usr/bin/env bash
# Поднимает базу данных и запускает программу в Docker.
# Первый запуск займёт пару минут (скачивание образов и сборка) — дальше быстрее.
set -e

echo "Поднимаем базу данных..."
docker compose up -d db

echo "Собираем и запускаем программу..."
docker compose run --rm app
